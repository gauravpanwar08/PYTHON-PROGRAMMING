# The code is importing a module named `NameMangling1` and then calling the `add` function from that module to calculate the sum of 40 and 50.

import NameMangling1
print("Sum",NameMangling1.add(40,50))
